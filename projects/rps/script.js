var scissors = function() {
  var userChoice = "scissors";
  return userChoice;
  confirm("deez nuts");
};

var rock = function() {
  var userChoice = "rock";
  return userChoice;
  confirm("deez nuts");
};

var paper = function() {
  var userChoice = "paper";
  return userChoice;
  confirm("deez nuts");
};

console.log(userChoice);
